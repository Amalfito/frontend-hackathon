import { Certificate } from "@/components/academy/certificate";

export const metadata = {
  title: "Certificat — Electra AI Academy",
};

export default function CertificatPage() {
  return <Certificate />;
}
